import { Box, Container } from "@mui/material";

type Props = {};

export default function Section2(props: Props) {
  return (
    <Container>
      <Box sx={{ height: "100vh" }}>Section2</Box>{" "}
    </Container>
  );
}
